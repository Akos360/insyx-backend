# PostgreSQL connection — used when running backend locally (outside Docker)
# When running inside Docker, DB_HOST is overridden to the service name in docker-compose.yml
DB_HOST=localhost
DB_PORT=5432
DB_USER=your_db_user
DB_PASSWORD=your_db_password
DB_NAME=insyx
